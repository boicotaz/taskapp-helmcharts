replicaCount: 2

image:
  repository: boicotaz/taskapp-frontend
  pullPolicy: Always
  tag: "5925f659d654e38be2d897ab75a0ecd5f3919afd"

service:
  type: ClusterIP
  port: 80

applicationPort: 80

ingress:
  enabled: true
  path: /
  pathType: Prefix

backend:
  serviceName: taskapp-backend
  servicePort: 80

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 10
  requestsPerSecond: 5

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}