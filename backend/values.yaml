replicaCount: 2

image:
  repository: boicotaz/taskapp-backend
  pullPolicy: Always
  tag: "49c134fdd0da41bf5cb9b5067d8356620495edfc"

applicationPort: 8080

readinessProbe:
  initialDelaySeconds: 5
  periodSeconds: 10
  failureThreshold: 3

service:
  type: ClusterIP
  port: 80

db:
  host: taskapp-database-cluster-rw
  port: 5432
  secretName: taskapp-database-cluster-secret
  name: taskapp

resources:
  requests:
    memory: "64Mi"
    cpu: "250m"
  limits:
    memory: "256Mi"
    cpu: "1000m"

nodeSelector: {}

tolerations: []

affinity: {}
